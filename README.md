# JamFactory

Static music theory and improvisation web app.

## Structure
- `index.html` — app
- `assets/pdfs/` — Real Book PDF assets loaded on demand

The PDFs are externalized from the HTML so GitHub/Vercel do not need a giant single HTML file.

## Deploy
Connect this repository to Vercel. No build command is required for the static app.
